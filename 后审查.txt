本轮不要新增任何功能。

请针对最近完成的 Web 设计系统、MemoryStore、ContextSelector 和 EventBus 做一次全面回归审查。

重点检查：

1. 旧长期记忆能否无损迁移，并且迁移只执行一次。
2. 所有仍假定 long_memory 是字符串列表的旧代码。
3. Web 记忆增删改是否兼容新的 MemoryItem 结构。
4. refine、compress、cleanup、memory audit 是否仍然有效。
5. ContextSelector 选中的内容是否与最终 prompt 完全一致。
6. 中文角色名、别名、代词、当前在场角色是否能正确召回。
7. Token 预算是否真实生效。
8. 选择器失败后的安全回退是否会产生重复上下文。
9. EventBus 当前实际接入了哪些事件，哪些只是定义但未使用。
10. 单个监听器报错是否真的不会影响 Telegram 回复。
11. 所有 Web 页面、弹窗、表单、按钮和移动端布局是否可用。
12. 浅色与深色主题下图标、表格、Badge、输入框是否清晰。
13. SVG sprite 在所有浏览器和所有模板中是否正常显示。
14. 检查提交说明与实际改动不一致的问题。
15. 运行现有测试，并补充针对以上问题的测试。

请先输出审查结果和具体证据，不要直接继续重构。